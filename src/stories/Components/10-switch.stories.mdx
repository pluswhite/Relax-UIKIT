import { Meta, ArgsTable, Story, Canvas } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import { Row } from '../../components/Grid';
import Icon from '../../components/Icon';
import Switcher from '../../components/Switch';

<Meta title="Components 基础组件/10 - Switch 开关" component={Switcher} />

# Switch 开关

## Basic Usage 基础用法

<Canvas>
  <Row>
    <Switcher className="demo-switch" checked />
    <Switcher className="demo-switch" checkedNode="ON" uncheckedNode="OFF" checked />
    <Switcher className="demo-switch" checkedNode="ON" uncheckedNode="OFF" />
    <Switcher
      className="demo-switch"
      checkedNode={<Icon icon="check" />}
      uncheckedNode={<Icon icon="times" />}
    />
  </Row>
</Canvas>

## API

<ArgsTable of={Switcher} />
